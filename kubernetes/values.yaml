replicaCount: 1

image:
  repository: woozl/frink-ui
  tag: "1.0.3"

resources:
  limits:
    cpu: 250m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

service:
  type: ClusterIP
  port: 8080

ingress:
  host: frink-ui.apps.renci.org
  path: /
  tls: true
  public: false
